<div class="profile-header">
  <%= current_user.name %> <br>
  <% if current_user.is_a? Undergrad%>
  <span> College Student</span>
  <!-- which school -->
  <%end%>
</div>


<!-- UNDERGRAD PROFILE  -->
<% if current_user.is_a? Undergrad%>

<div id="nav">
  <div class="btn-group btn-group-justified" role="group" aria-label="...">
    <div class="btn-group" role="group">
      <button type="button" class="btn btn-default"> Photos</button>
    </div>
    <div class="btn-group" role="group">
      <button type="button" class="btn btn-default"> A Day in the Life</button>
    </div>
  </div>
</div>

<div class="photo-collection">
  <% @photos = Photo.where(undergrad_id: current_user.id) %>
  <% @photos.reverse.each do |photo|%>
  <div class="photo">
    <img src="<%= photo.url%>">
  </div>
  <%end%>
</div>
<%end%>


<!-- STUDENT PROFILE -->
<!-- view and CRUD bookmarks -->
<!-- view and CRUD follows -->

<% if current_user.is_a? Student %>


<%end%>
